<?php
/**
 * AI Amigos Remediation uninstall handler.
 *
 * The plugin creates no options, posts, users, metadata, or custom tables.
 * There is intentionally no plugin data to delete.
 */

if ( ! defined( 'WP_UNINSTALL_PLUGIN' ) ) {
	exit;
}
